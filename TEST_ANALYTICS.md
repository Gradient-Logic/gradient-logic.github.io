# Testing Analytics

Measurement ID: `G-MQTH1GW250`

1. Run `npm run preview` (or visit production).
2. Open DevTools → Network → filter `gtag` / `collect`.
3. In GA4 → Reports → Realtime, confirm yourself as a visitor.

Outbound clicks (Calendly, mailto, LinkedIn) show up when Enhanced measurement → Outbound clicks is on in the data stream.
