/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Next builds to ./out
  // images: { unoptimized: true }, // if you ever use <Image>
};
module.exports = nextConfig;
